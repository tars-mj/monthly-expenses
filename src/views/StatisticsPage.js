import React, { useContext } from 'react';
import PageTemplate from '../templates/PageTemplate';

const StatisticsPage = () => {
  return <PageTemplate>statistics</PageTemplate>;
};

export default StatisticsPage;
